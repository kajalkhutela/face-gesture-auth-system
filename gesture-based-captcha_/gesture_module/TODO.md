# Gesture Module TODO\n\n## Fix train_model.py path error (Approved Plan) ✅\n- [x] Step 1: Add `import config` to train_model.py\n- [x] Step 2: Replace `data_dir = "dataset"` with `data_dir = config.DATA_DIR`\n- [ ] Step 3: Test run `python gesture-based-captcha_/gesture_module/train_model.py`\n- [ ] Step 4: Verify model training completes and saves gesture_model.h5

